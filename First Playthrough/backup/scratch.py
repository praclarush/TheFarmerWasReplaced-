import actions
import factory
import helpers

#rebuild main loop
#check each item, if its below min set for item add to 
#list
#for though list getting item until items meet min
#loop
#if no items in list exit loop

#plant tree
#if row % 2 == 0 and column % 2 == 1 then plant tree
#else if row % 2 == 1 and column % 2 == 0 plant tree

#function to move to x,y location
#function to travel a specific square

#function to hargest and then replant before moving